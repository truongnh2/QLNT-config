{
  "version": "1.0.0",
  "description": "QLNT Server Configuration - Shared config for Client (Flutter) and Server (Spring Boot)",
  "environments": {
    "development": {
      "baseUrl": "http://10.0.2.2:8081/api/v1",
      "description": "Android Emulator → host machine (port 8081)"
    },
    "staging": {
      "baseUrl": "https://staging.qlnt.example.com/api/v1",
      "description": "Staging server"
    },
    "production": {
      "baseUrl": "https://rental.io.vn/api/v1",
      "description": "Production server"
    }
  },
  "server": {
    "port": 8081,
    "contextPath": "/api/v1",
    "note": ""
  }
}
